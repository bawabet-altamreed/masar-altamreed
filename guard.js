import{wait,profile,logout}from"../firebase/auth.js";export async function guard(role,sub=false){let u=await wait();if(!u){location.href='../login.html';throw Error('login')}let p=await profile(u.uid);if(!p||p.role!==role||p.accountStatus!=='active'){await logout();throw Error('denied')}if(sub&&p.subscriptionStatus!=='active'){alert('الاشتراك غير نشط');await logout();throw Error('sub')}return{u,p}}export const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));export const date=v=>{let d=v?.toDate?v.toDate():new Date(v);return isNaN(d)?'—':d.toLocaleDateString('ar-EG')};
