import { guard } from "../core/guard.js"; import { shell } from "../core/layout.js"; export function start(title,render){guard("admin");window.addEventListener("masar-ready",e=>document.querySelector("#root").innerHTML=shell("admin",title,render(e.detail)));}