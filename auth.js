import{auth,db,ADMIN_UID}from"./config.js";import{signInWithEmailAndPassword,signOut,onAuthStateChanged}from"https://www.gstatic.com/firebasejs/10.12.5/firebase-auth.js";import{doc,getDoc}from"https://www.gstatic.com/firebasejs/10.12.5/firebase-firestore.js";
export const internalEmail=c=>`${String(c||'').trim().toLowerCase().replace(/[^a-z0-9_-]/g,'')}@accounts.masar.local`;export const loginByCode=(c,p)=>signInWithEmailAndPassword(auth,internalEmail(c),p);export const loginAdmin=(e,p)=>signInWithEmailAndPassword(auth,e.trim(),p);export async function profile(uid){let s=await getDoc(doc(db,'users',uid));return s.exists()?{uid,...s.data()}:uid===ADMIN_UID?{uid,role:'admin',name:'مدير المنصة',accountStatus:'active'}:null}export const wait=()=>new Promise(r=>onAuthStateChanged(auth,r));export async function logout(){await signOut(auth);location.href=location.pathname.includes('/')?'../login.html':'login.html'};
