import { start } from "./common.js"; import { call } from "../firebase/functions.js";
start("إدارة الطلاب",()=>`<div class="card"><h2>إنشاء طالب</h2><form id="createStudent"><input class="input" name="name" placeholder="اسم الطالب" required><input class="input" name="stage" placeholder="المرحلة: الصف الثاني الثانوي التمريض" required><input class="input" name="code" placeholder="كود الاشتراك" required><input class="input" name="password" type="password" placeholder="كلمة المرور" required><input class="input" name="endDate" type="date" required><button class="btn">إنشاء الحساب</button></form><p id="out"></p></div>`);
document.addEventListener("submit",async e=>{if(e.target.id!=="createStudent")return;e.preventDefault();const x=Object.fromEntries(new FormData(e.target));try{await call("createStudent",x);document.querySelector("#out").textContent="تم إنشاء الطالب بأمان."}catch(err){document.querySelector("#out").textContent=err.message}});